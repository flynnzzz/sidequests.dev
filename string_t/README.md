# string_t.c

My attempt at a custom string type in C.

## 1. requirements

- a C compiler

## 2. notes

- The main.c file can be compiled into a rudimentary *string replace* CLI tool
  that makes use of my `string_replace` function.
